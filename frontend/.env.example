# ========================================================
# QBS-360 - Frontend Environment Configuration
# ========================================================

# Custom Backend API Base URL (Optional)
# If left unset, defaults to '/api' (ideal when served on same domain or behind Nginx reverse proxy).
# For decoupled or cross-domain hosting (e.g. Vercel frontend calling Railway/VPS backend):
# VITE_API_BASE_URL=https://whatsq.qiyambusinesssolutions.com
